set(CMAKE_CXX_STANDARD 17)
include_directories(include)
file(GLOB_RECURSE manda_includes include/*.hpp)
file(GLOB_RECURSE bytecode_srcs src/bytecode/*.cpp)
add_library(manda ${manda_includes} ${bytecode_srcs})